# Calendar of Desires (Календарь Желаний)

Прогрессивное веб-приложение (PWA) для ежедневной работы с желаниями через ритуалы и рефлексию.

## 📋 Описание

Calendar of Desires — это приложение, которое помогает вам работать с вашими желаниями через ежедневные ритуалы. Вы создаете желание, описываете его эмоциональную значимость, и каждый день отвечаете на вопросы, которые помогают оставаться в контакте с вашим желанием и отслеживать прогресс.

## ✨ Основные возможности

- **Создание желаний**: Создавайте желания с названием, эмоциональным описанием, дедлайном и визуальным образом
- **Ежедневный ритуал**: Каждый день получайте случайный вопрос для рефлексии о вашем желании
- **Дневниковые записи**: Ведите записи о своем прогрессе с текстом и оценкой близости к желанию (1-10)
- **История**: Просматривайте историю всех ваших записей в виде временной шкалы
- **Экспорт данных**: Экспортируйте свой путь в текстовый файл для сохранения или анализа
- **Офлайн-режим**: Работает без интернета благодаря PWA и локальному хранилищу
- **Установка на устройство**: Можно установить как нативное приложение

## 🛠 Технологии

- **React 19** — UI библиотека
- **TypeScript** — типизированный JavaScript
- **Vite** — сборщик и dev-сервер
- **Dexie** — обертка над IndexedDB для локального хранения данных
- **Vite PWA Plugin** — поддержка PWA функциональности
- **React Router DOM** — маршрутизация (в зависимостях)

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── DailyRitual/     # Компонент ежедневного ритуала
│   ├── DesireForm/      # Форма создания/редактирования желания
│   ├── HistoryTimeline/ # Временная шкала истории записей
│   ├── SimpleDailyRitual.tsx      # Упрощенный компонент ритуала
│   ├── SimpleDesireForm.tsx       # Упрощенная форма желания
│   └── SimpleHistoryTimeline.tsx  # Упрощенная временная шкала
├── services/
│   └── db.ts            # Сервис работы с базой данных (Dexie)
├── types/
│   └── index.ts         # TypeScript типы и интерфейсы
├── utils/
│   ├── dailyQuestions.ts # Генерация вопросов дня
│   ├── date.ts          # Утилиты для работы с датами
│   └── export.ts        # Экспорт данных
├── App.tsx              # Главный компонент приложения
└── main.tsx             # Точка входа
```

## 🚀 Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр продакшен сборки

```bash
npm run preview
```

### Линтинг

```bash
npm run lint
```

## 💾 Хранение данных

Приложение использует **IndexedDB** через библиотеку **Dexie** для локального хранения всех данных. Данные хранятся в браузере и не отправляются на сервер.

### Структура базы данных

- **desires** — таблица желаний
  - `id` — уникальный идентификатор
  - `title` — название желания
  - `description` — эмоциональное описание
  - `deadline` — дедлайн (опционально)
  - `imageUrl` — URL изображения (base64 или URL)
  - `createdAt` — дата создания
  - `isActive` — флаг активного желания (только одно может быть активным)

- **dailyEntries** — таблица ежедневных записей
  - `id` — уникальный идентификатор
  - `desireId` — ID связанного желания
  - `date` — дата записи (YYYY-MM-DD)
  - `type` — тип записи: 'full' (полная) или 'view' (только просмотр)
  - `text` — текст записи
  - `closenessRating` — оценка близости к желанию (1-10)
  - `createdAt` — временная метка создания

## 🎯 Основные функции

### Создание желания

1. Заполните форму с названием желания
2. Опишите эмоциональную значимость ("Зачем это тебе?")
3. При необходимости установите дедлайн
4. Загрузите визуальный образ (опционально)

### Ежедневный ритуал

1. Каждый день вы получаете случайный вопрос о вашем желании
2. Можете написать текст-рефлексию
3. Оцените близость к желанию по шкале от 1 до 10
4. Сохраните запись или просто отметьте момент присутствия

### История

Просматривайте все ваши записи в хронологическом порядке на временной шкале. Вы можете экспортировать весь ваш путь в текстовый файл для сохранения или дальнейшего анализа.

## 🔧 Конфигурация PWA

PWA настроена через `vite.config.ts` с использованием `vite-plugin-pwa`. Приложение поддерживает:

- Автоматическое обновление сервис-воркера
- Офлайн-режим
- Установку на устройство
- Кэширование ресурсов

## 📝 Скрипты

- `npm run dev` — запуск dev-сервера
- `npm run build` — сборка для продакшена
- `npm run preview` — предпросмотр продакшен сборки
- `npm run lint` — проверка кода линтером

## 🎨 Особенности UI

- Минималистичный дизайн
- Адаптивная верстка
- Темная/светлая тема (базовая поддержка)
- Интуитивная навигация между разделами

## 📄 Лицензия

Проект является приватным (private: true в package.json).

## 🔮 Будущие улучшения

- Статистика и аналитика прогресса
- Напоминания о ежедневном ритуале
- Поддержка нескольких активных желаний
- Синхронизация между устройствами (опционально)

---

Создано с ❤️ для работы с желаниями и личностного роста
