# 📋 Процесс работы с проектом Calendar of Desires

## 🎯 Цель документа
Этот документ описывает стандартный процесс работы с проектом, чтобы при каждой новой сессии можно было быстро понять текущее состояние и правильно работать с git и деплоем.

---

## 📥 ШАГ 1: Обновление локалки с гита (начало работы)

Когда начинаешь работу, **всегда сначала обновляй локалку до состояния `origin/main`**:

```bash
# 1. Проверить текущее состояние
git status

# 2. Если есть локальные изменения - сохранить их (на всякий случай)
git stash push -u -m "Local changes before sync - $(date)"

# 3. Обновить до состояния origin/main
git fetch origin --prune
git reset --hard origin/main

# 4. Удалить неотслеживаемые файлы (временные файлы)
git clean -fd

# 5. Проверить что все чисто
git status
# Должно быть: "Your branch is up to date with 'origin/main'. nothing to commit, working tree clean"
```

**Важно:** После этого локалка будет **точно соответствовать** тому, что в гите и что работает на PWA.

---

## 🔍 ШАГ 2: Проверка текущего состояния проекта

### Последние изменения в гите:
```bash
# Посмотреть последние коммиты
git log --oneline -15 origin/main

# Посмотреть что изменилось в последнем коммите
git show origin/main --stat
```

### Текущая версия проекта:
- **Последний коммит:** `80b734d` - "CI: use Node 22 for Vite 7"
- **Главный экран:** LifeWheel (колесо жизни)
- **Деплой:** Автоматический через GitHub Actions при пуше в `main`
- **PWA URL:** https://tilkerman.github.io/calendar-of-desires/

---

## 💻 ШАГ 3: Локальная разработка

### Запуск dev-сервера:
```bash
npm install          # Если нужно обновить зависимости
npm run dev          # Запуск на http://localhost:5173
npm run preview      # Просмотр production build на http://localhost:4173
```

### Структура проекта:
- `src/App.tsx` - главный роутинг (wheel/list/form/detail)
- `src/components/LifeWheel/` - главный экран (колесо жизни)
- `src/components/DesiresList/` - список желаний
- `src/components/DesireDetail/` - детали желания
- `src/components/DesireForm/` - форма создания/редактирования

---

## 📤 ШАГ 4: Выгрузка изменений обратно в гит

После того как все изменения сделаны и протестированы локально:

### 1. Проверить что изменилось:
```bash
git status
git diff
```

### 2. Добавить изменения:
```bash
# Добавить все измененные файлы
git add .

# Или добавить конкретные файлы
git add src/App.tsx src/components/...
```

### 3. Закоммитить:
```bash
git commit -m "Описание изменений: что было сделано"
```

**Примеры хороших сообщений коммитов:**
- `feat: добавлена функция X`
- `fix: исправлена ошибка Y`
- `refactor: улучшена структура компонента Z`
- `style: обновлены стили для экрана X`

### 4. Запушить в гит:
```bash
git push origin main
```

**Важно:** После пуша в `main` автоматически запустится GitHub Actions workflow, который:
1. Соберет проект (`npm run build`)
2. Задеплоит в GitHub Pages
3. Обновит PWA на https://tilkerman.github.io/calendar-of-desires/

---

## 🚀 ШАГ 5: Проверка деплоя

### Автоматический деплой:
После пуша в `main`:
1. Перейти в GitHub: https://github.com/Tilkerman/calendar-of-desires/actions
2. Проверить что workflow "Deploy to GitHub Pages" успешно выполнился (зеленая галочка)
3. Обычно деплой занимает 1-2 минуты

### Проверка на сайте:
- Открыть: https://tilkerman.github.io/calendar-of-desires/
- Проверить что изменения применились
- Если PWA установлено на телефоне - может потребоваться обновление (закрыть/открыть приложение)

---

## 📝 Чеклист начала работы (для AI ассистента)

При начале новой сессии работы с проектом:

- [ ] Выполнить ШАГ 1 (обновление локалки с гита)
- [ ] Проверить последние коммиты (ШАГ 2)
- [ ] Убедиться что `git status` показывает чистое состояние
- [ ] Запустить `npm run dev` и проверить что проект работает
- [ ] Прочитать этот документ чтобы понять текущий процесс

---

## 📝 Чеклист завершения работы (для AI ассистента)

Перед завершением сессии:

- [ ] Все изменения закоммичены и запушены (ШАГ 4)
- [ ] GitHub Actions workflow успешно выполнился
- [ ] Изменения видны на https://tilkerman.github.io/calendar-of-desires/
- [ ] Обновлен этот документ если процесс изменился

---

## 🔧 Полезные команды

```bash
# Посмотреть историю коммитов
git log --oneline -20

# Посмотреть изменения в конкретном файле
git diff src/App.tsx

# Отменить локальные изменения (если что-то пошло не так)
git reset --hard origin/main

# Посмотреть что в stash (сохраненные изменения)
git stash list

# Вернуть изменения из stash
git stash pop
```

---

## ⚠️ Важные замечания

1. **Всегда начинай с обновления локалки** - это гарантирует что работаешь с актуальной версией
2. **Не коммитьть незавершенную работу** - лучше использовать stash
3. **После пуша проверяй деплой** - GitHub Actions должен успешно завершиться
4. **PWA может кэшировать старую версию** - на телефоне может потребоваться переустановка PWA

---

## 📅 История последних изменений

Последние коммиты в `origin/main`:
- `80b734d` - CI: use Node 22 for Vite 7
- `b8a8410` - CI: switch to official GitHub Pages deploy
- `0d38b51` - CI: fix GitHub Pages deploy (actions-gh-pages v4)
- `9c66c40` - LifeWheel area navigation + mobile dots overflow fix
- `4bc5ed0` - Fix build + ensure area picker flow deployable
- `c1ebed9` - LifeWheel + all desires list navigation

---

**Дата последнего обновления документа:** 2025-01-06

