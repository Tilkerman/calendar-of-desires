# 📧 Настройка отправки обратной связи

Приложение поддерживает отправку отзывов на **email** и в **Telegram**.

## 📮 Вариант 1: Email (mailto)

**Работает сразу без настройки!**

После отправки отзыва откроется почтовый клиент (Gmail, Outlook, Mail и т.д.) с предзаполненным письмом.

### Настройка (опционально)

Если хочешь, чтобы письмо автоматически отправлялось на конкретный email, создай файл `.env` в корне проекта:

```env
VITE_FEEDBACK_EMAIL=your-email@example.com
```

Если не указать email, пользователь сам введёт адрес получателя в почтовом клиенте.

## 📱 Вариант 2: Telegram Bot

Для автоматической отправки отзывов в Telegram нужно создать бота.

### Шаг 1: Создай Telegram бота

1. Открой Telegram и найди [@BotFather](https://t.me/BotFather)
2. Отправь команду `/newbot`
3. Следуй инструкциям и получи токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Узнай свой Chat ID

1. Найди бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправь ему любое сообщение
3. Он вернёт твой Chat ID (число, например `123456789`)

### Шаг 3: Настрой переменные окружения

Создай файл `.env` в корне проекта:

```env
VITE_TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
VITE_TELEGRAM_CHAT_ID=123456789
```

### Шаг 4: Пересобери проект

```bash
npm run build
```

## 🔄 Как это работает

1. Пользователь отправляет отзыв
2. Отзыв сохраняется в IndexedDB (локально)
3. Появляется вопрос: "Отправить отзыв на email?"
4. Если "Да":
   - Если настроен Telegram Bot → отправка в Telegram
   - Если Telegram не настроен → открывается почтовый клиент (mailto)
5. Если "Нет" → отзыв остаётся только в локальной БД

## 📝 Примечания

- Все отзывы **всегда сохраняются локально** в IndexedDB
- Отзывы можно экспортировать вместе с остальными данными (Экспорт / Импорт)
- Если Telegram Bot не настроен, автоматически используется mailto
- Email работает на всех устройствах без дополнительных настроек

