# Calendar of Desires (Календарь Желаний)

Прогрессивное веб-приложение (PWA) для ежедневной работы с желаниями через ритуалы и рефлексию.

## 📋 Описание

Calendar of Desires — это приложение, которое помогает вам работать с вашими желаниями через ежедневные ритуалы. Вы создаете желание, описываете его эмоциональную значимость, и каждый день отвечаете на вопросы, которые помогают оставаться в контакте с вашим желанием и отслеживать прогресс.

## ✨ Основные возможности

- **Создание желаний**: Создавайте желания с названием, описанием образа, эмоциональным состоянием и визуальным образом
- **Фокус на желании**: Выбирайте одно желание "в фокусе" на день - то, к которому вы сегодня возвращаетесь
- **Ежедневные контакты**: Три типа контактов с желанием:
  - **Запись** — текстовая запись о желании
  - **Мысль** — мысль, связанная с желанием
  - **Шаг** — маленький шаг или намерение
- **Индикаторы контакта**: Визуальные индикаторы показывают регулярность контакта за последние 7 дней
- **История**: Просматривайте всю историю контактов с желанием
- **Офлайн-режим**: Работает без интернета благодаря PWA и локальному хранилищу (IndexedDB)
- **Установка на устройство**: Можно установить как нативное приложение

## 🛠 Технологии

- **React 19** — UI библиотека
- **TypeScript** — типизированный JavaScript
- **Vite** — сборщик и dev-сервер
- **Dexie** — обертка над IndexedDB для локального хранения данных
- **Vite PWA Plugin** — поддержка PWA функциональности
- **React Router DOM** — маршрутизация (в зависимостях)

## 📁 Структура проекта

```
src/
├── components/              # React компоненты
│   ├── ContactIndicators/   # Индикаторы контакта за 7 дней
│   ├── DesireDetail/         # Детальный экран желания с контактами
│   ├── DesireForm/           # Форма создания/редактирования желания
│   ├── DesiresList/          # Список всех желаний
│   ├── Header/               # Шапка приложения с переключением темы
│   └── WelcomeScreen/        # Экран приветствия
├── services/
│   └── db.ts                 # Сервис работы с базой данных (Dexie)
├── types/
│   └── index.ts              # TypeScript типы и интерфейсы
├── utils/
│   ├── contactIndicators.ts  # Утилиты для индикаторов контакта
│   └── date.ts               # Утилиты для работы с датами
├── App.tsx                   # Главный компонент приложения
└── main.tsx                  # Точка входа
```

## 🚀 Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

**Приложение будет доступно по адресу:** [`http://localhost:5173`](http://localhost:5173)

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр продакшен сборки

```bash
npm run preview
```

### Линтинг

```bash
npm run lint
```

## 💾 Хранение данных

Приложение использует **IndexedDB** через библиотеку **Dexie** для локального хранения всех данных. Данные хранятся в браузере и не отправляются на сервер.

### Структура базы данных

- **desires** — таблица желаний
  - `id` — уникальный идентификатор
  - `title` — название желания
  - `description` — эмоциональное описание ("Как ты хочешь себя чувствовать?")
  - `details` — подробное описание образа желания
  - `imageUrl` — URL изображения (base64 или URL, legacy)
  - `images` — массив изображений с порядком отображения
  - `createdAt` — дата создания
  - `isActive` — флаг активного желания "в фокусе" (только одно может быть активным)

- **contacts** — таблица ежедневных контактов
  - `id` — уникальный идентификатор
  - `desireId` — ID связанного желания
  - `date` — дата контакта (YYYY-MM-DD)
  - `type` — тип контакта: 'entry' (запись), 'thought' (мысль), 'step' (шаг)
  - `text` — текст контакта (опционально)
  - `createdAt` — временная метка создания

## 🎯 Основные функции

### Создание желания

1. Заполните название желания
2. Опишите образ желания ("Опиши своё желание")
3. Опишите эмоциональное состояние ("Как ты хочешь себя чувствовать?")
4. Загрузите визуальный образ (опционально)

### Работа с желаниями

1. **Выбор фокуса**: Выберите одно желание "в фокусе" на день (до 23:00)
2. **Ежедневные контакты**: Добавляйте контакты трёх типов:
   - **Запись** — текстовая запись о желании
   - **Мысль** — мысль, связанная с желанием
   - **Шаг** — маленький шаг или намерение
3. **Индикаторы**: Смотрите визуальные индикаторы регулярности контакта за последние 7 дней
4. **История**: Просматривайте всю историю контактов с желанием

### Автоматика

- После 23:00 все флаги "в фокусе" сбрасываются
- Выбор желания "в фокусе" автоматически создаёт контакт типа "мысль"
- Если у активного желания нет контакта за сегодня, автоматически создаётся контакт "мысль"

## 🔧 Конфигурация PWA

PWA настроена через `vite.config.ts` с использованием `vite-plugin-pwa`. Приложение поддерживает:

- Автоматическое обновление сервис-воркера
- Офлайн-режим
- Установку на устройство
- Кэширование ресурсов

## 📝 Скрипты

- `npm run dev` — запуск dev-сервера
- `npm run build` — сборка для продакшена
- `npm run preview` — предпросмотр продакшен сборки
- `npm run lint` — проверка кода линтером
- `npm run i18n:check` — проверка, что в `.tsx` нет хардкод-текста (всё через RU/EN i18n)

## 🌍 Языки (RU/EN)

Проект поддерживает 2 языка: русский и английский.

- Переключение языка находится в шапке приложения.
- Все пользовательские тексты должны идти через i18n (`t('...')`).
- Правила зафиксированы в `I18N_RULES.md`.

## 🎨 Особенности UI

- Минималистичный дизайн
- Адаптивная верстка
- Темная/светлая тема (базовая поддержка)
- Интуитивная навигация между разделами

## 📄 Лицензия

Проект является приватным (private: true в package.json).

## 🔮 Будущие улучшения

- Статистика и аналитика прогресса
- Напоминания о ежедневном ритуале
- Поддержка нескольких активных желаний
- Синхронизация между устройствами (опционально)

---

Создано с ❤️ для работы с желаниями и личностного роста
